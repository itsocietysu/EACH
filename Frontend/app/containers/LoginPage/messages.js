/*
 * LoginPage Messages
 *
 * This contains all the text for the LoginPage component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  registration: {
    id: 'each.containers.LoginPage.registration',
    defaultMessage: 'not registered yet?',
  },
});
